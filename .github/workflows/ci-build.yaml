name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.21'
      - name: Run tests
        working-directory: llm-mock-server
        run: go test ./...

  build:
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
            name: llm-mock-server-linux-amd64
          - goos: windows
            goarch: amd64
            name: llm-mock-server-windows-amd64.exe

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Build
        env:
          CGO_ENABLED: "0"
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        working-directory: llm-mock-server
        run: go build -ldflags="-s -w" -o ${{ matrix.name }} ./cmd/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: llm-mock-server/${{ matrix.name }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Get commit message
        id: commit
        run: |
          echo "msg<<EOF" >> "$GITHUB_OUTPUT"
          git log -1 --format=%B >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: bins

      - name: Generate SHA256 checksums
        run: |
          sha256sum \
            bins/llm-mock-server-linux-amd64/llm-mock-server-linux-amd64 \
            bins/llm-mock-server-windows-amd64.exe/llm-mock-server-windows-amd64.exe \
            | sed 's|bins/[^/]*/||g' > bins/SHA256SUMS.txt
          cat bins/SHA256SUMS.txt

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          body: ${{ steps.commit.outputs.msg }}
          files: |
            bins/llm-mock-server-linux-amd64/llm-mock-server-linux-amd64
            bins/llm-mock-server-windows-amd64.exe/llm-mock-server-windows-amd64.exe
            bins/SHA256SUMS.txt
